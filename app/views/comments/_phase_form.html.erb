<%= form_for [@phase.project_lead, @phase, Comment.includes(:phase).new] do |form| %>
  <div class="field">
    <%= form.text_area :body, class: 'form-control', placeholder: 'Add a Comment' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
