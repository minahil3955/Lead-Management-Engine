<%= form_for [@project_lead, Comment.includes(:project_lead).new] do |form| %>
  <div class="field">
    <%= form.text_area :body, class: 'form-control', placeholder: 'Add a Comment' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
