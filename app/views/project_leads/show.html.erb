<div class = 'my-2 mx-3'>
<p id="notice"><%= notice %></p>

<p>
  <strong>Assignee:</strong>
  <%= @project_lead.user.name %>
</p>

<p>
  <strong>Client:</strong>
  <%= @project_lead.client.name %>
</p>

<p>
  <strong>Name:</strong>
  <%= @project_lead.name %>
</p>

<p>
  <strong>Platform:</strong>
  <%= @project_lead.platform %>
</p>

<p>
  <strong>Sale Status:</strong>
  <%= @project_lead.is_sale %>
</p>

<p>
  <strong>Created Date</strong>
  <td><%= @project_lead.created_at.to_date %></td>
</p>

<button  class="btn btn-dark"><%= link_to 'Show Phases', project_lead_phases_path(@project_lead) %></button>

<%= render partial: 'comments/comments', collection: @project_lead.comments, as: :comment %>
<%= render partial: 'comments/lead_form', locals: {commentable: @project_lead}%>

</div>
